# 現在の実装状況

最終更新: 2026-09-19

## 状態

初期リリースは完了しています。基本的な動作確認を完了しています。

## 実装済みの機能

- 選択タブの URL から正規表現の第 1 キャプチャでファイル名を抽出する。
- URL 検証、重複排除、Native Messaging による安全なコピーを行う。
- コピー前にコピー元・先を検証し、コピー先の既存ファイルを上書きしない。
- 全コピー成功時だけ、設定に応じて対象タブを閉じる。
- macOS、Linux、Windows 向けの Native Host 登録・削除スクリプトを提供する。

## 直近の検証結果

- `npm run format:check`、`npm run lint`、`npm test`、`npm run build`、`git diff --check` が成功した。
- 自動テスト 34 件と基本的な実機動作確認を完了した。

## 次に着手する項目

現時点で予定された作業はありません。仕様変更または不具合対応が必要になった場合は、このファイルを更新してから着手します。
